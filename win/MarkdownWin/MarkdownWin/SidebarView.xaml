<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarkdownWin.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarkdownWin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" HorizontalAlignment="Left" Background="Transparent">
            <AppBarButton Icon="Folder" Label="Open Folder…" Click="OnOpenFolderClick" ToolTipService.ToolTip="Open a folder of Markdown files">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control,Shift" Key="O" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarButton Label="Assistant" Click="OnAssistantClick" ToolTipService.ToolTip="Ask the assistant about this folder">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xF4A9;" />
                </AppBarButton.Icon>
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="J" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
        </CommandBar>

        <!-- Three mutually-exclusive states, toggled by Visibility in code-behind. -->
        <Grid Grid.Row="1">
            <!-- Search results -->
            <Grid x:Name="SearchResultsPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel x:Name="SearchEmptyState" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="24,40,24,0" Spacing="8">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE721;" FontSize="32" HorizontalAlignment="Center" Opacity="0.6" />
                    <TextBlock x:Name="SearchEmptyStateTitle" Text="No Results" HorizontalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock x:Name="SearchEmptyStateDescription" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Opacity="0.7" />
                </StackPanel>
                <ListView x:Name="SearchResultsList" Grid.Row="1" SelectionMode="Single" SelectionChanged="OnSearchResultSelected">
                    <ListView.Header>
                        <TextBlock x:Name="SearchResultsHeader" Margin="12,4" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" />
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:SearchHit">
                            <StackPanel Spacing="2" Margin="0,4">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE8A5;" FontSize="14" />
                                    <TextBlock Text="{x:Bind Name}" />
                                </StackPanel>
                                <ItemsControl ItemsSource="{x:Bind Snippets}" Margin="20,0,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{x:Bind}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- File tree -->
            <ListView x:Name="FileTreeList" Visibility="Collapsed" SelectionMode="Single" SelectionChanged="OnFileTreeSelectionChanged" RightTapped="OnFileTreeRightTapped">
                <ListView.Header>
                    <TextBlock x:Name="FileTreeHeader" Margin="12,4" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" />
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:FileRow">
                        <Grid Margin="{x:Bind Indent}" ColumnSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0" FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="{x:Bind Chevron}" FontSize="10" Visibility="{x:Bind ChevronVisibility}" Tapped="OnChevronTapped" />
                            <FontIcon Grid.Column="1" FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="{x:Bind Icon}" FontSize="14" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Node.Name}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Empty state -->
            <StackPanel x:Name="EmptyStatePanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8" Margin="24">
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE8B7;" FontSize="32" HorizontalAlignment="Center" Opacity="0.6" />
                <TextBlock Text="No Folder Open" HorizontalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock Text="Open a folder to browse its Markdown files." HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Opacity="0.7" />
                <Button Content="Open Folder…" HorizontalAlignment="Center" Click="OnOpenFolderClick" />
            </StackPanel>
        </Grid>

        <Border x:Name="ErrorBanner" Grid.Row="2" Visibility="Collapsed" Padding="8">
            <TextBlock x:Name="ErrorBannerText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" />
        </Border>

        <Border Grid.Row="3" Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <DropDownButton x:Name="AccountRow" Grid.Row="4" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Margin="4">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <PersonPicture x:Name="AccountAvatar" Grid.Column="0" Width="24" Height="24" />
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="0">
                    <TextBlock x:Name="AccountNameText" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="AccountEmailText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </Grid>
            <DropDownButton.Flyout>
                <MenuFlyout x:Name="AccountFlyout" Placement="TopEdgeAlignedLeft" Opening="OnAccountFlyoutOpening" />
            </DropDownButton.Flyout>
        </DropDownButton>
    </Grid>
</UserControl>
