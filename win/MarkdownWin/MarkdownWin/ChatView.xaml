<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarkdownWin.ChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarkdownWin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="UserMessageTemplate" x:DataType="local:ChatMessage">
            <Border HorizontalAlignment="Right" MaxWidth="320" Padding="10" CornerRadius="10" Margin="0,4" Background="{ThemeResource AccentFillColorDefaultBrush}">
                <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="AssistantMessageTemplate" x:DataType="local:ChatMessage">
            <Border HorizontalAlignment="Left" MaxWidth="320" Padding="10" CornerRadius="10" Margin="0,4">
                <TextBlock Text="{x:Bind DisplayText, Mode=OneWay}" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="ToolMessageTemplate" x:DataType="local:ChatMessage">
            <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,4">
                <ProgressRing IsActive="True" Width="14" Height="14" Visibility="{x:Bind IsToolRunning, Mode=OneWay}" />
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE73E;" FontSize="14" Visibility="{x:Bind IsToolSucceeded, Mode=OneWay}" />
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE711;" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind IsToolFailed, Mode=OneWay}" />
                <TextBlock Text="{x:Bind ToolName}" FontFamily="Cascadia Mono, Consolas" FontSize="12" />
                <TextBlock Text="{x:Bind ToolDetail, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextTrimming="CharacterEllipsis" MaxWidth="200" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="NoticeMessageTemplate" x:DataType="local:ChatMessage">
            <Border Margin="0,4" Padding="8" CornerRadius="6" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" HorizontalAlignment="Stretch">
                <TextBlock Text="{x:Bind Text}" Opacity="0.8" TextWrapping="Wrap" />
            </Border>
        </DataTemplate>

        <local:ChatMessageTemplateSelector
            x:Key="MessageTemplateSelector"
            UserTemplate="{StaticResource UserMessageTemplate}"
            AssistantTemplate="{StaticResource AssistantMessageTemplate}"
            ToolTemplate="{StaticResource ToolMessageTemplate}"
            NoticeTemplate="{StaticResource NoticeMessageTemplate}" />
    </UserControl.Resources>

    <Grid Width="420" Height="560" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="12" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6">
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xF4A9;" />
                <TextBlock Text="Assistant" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
            </StackPanel>
            <Button Grid.Column="2" Content="Done" Click="OnDoneClick" />
        </Grid>

        <Border Grid.Row="1" Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <Grid Grid.Row="2">
            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8" Margin="24">
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xF4A9;" FontSize="32" HorizontalAlignment="Center" Opacity="0.6" />
                <TextBlock Text="Ask About Your Notes" HorizontalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Name="EmptyStateDescription" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Opacity="0.7" MaxWidth="300" />
            </StackPanel>

            <ListView
                x:Name="Transcript"
                Padding="12"
                SelectionMode="None"
                ItemTemplateSelector="{StaticResource MessageTemplateSelector}" />
        </Grid>

        <Border Grid.Row="3" Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <StackPanel Grid.Row="4" Margin="12" Spacing="6">
            <TextBlock x:Name="ErrorText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Visibility="Collapsed" TextWrapping="Wrap" />
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    x:Name="Composer"
                    Grid.Column="0"
                    PlaceholderText="Ask the assistant…"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    MaxHeight="120"
                    KeyDown="OnComposerKeyDown"
                    TextChanged="OnComposerTextChanged" />
                <Button Grid.Column="1" x:Name="SendButton" VerticalAlignment="Bottom" Click="OnSendClick" ToolTipService.ToolTip="Send (Ctrl+Enter)">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE725;" />
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="Enter" />
                    </Button.KeyboardAccelerators>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
